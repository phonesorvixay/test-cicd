pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // dir('develop') {
                    sh 'npm install'
                //     // sh 'npm build'
                // }
                //asdfasdfasdf
            }
        }
    }
}