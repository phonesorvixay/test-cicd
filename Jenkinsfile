pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // dir('develop') {
                    sh 'npm install'
                    sh 'node server.js'
                //     // sh 'npm build'
                // }
                //asdfasdfasdf
            }
        }
    }
}